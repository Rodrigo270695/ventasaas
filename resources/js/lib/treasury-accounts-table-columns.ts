/** Anchuras compartidas en tablas CxP / CxC (`table-fixed`). */

export const treasuryAccountsTh = 'px-2 py-1.5 text-[10px]';

export const treasuryAccountsTd = 'px-2 py-1.5 text-xs';

export const treasuryColInvoice = 'w-[7.5rem]';

/** Proveedor o cliente — ancho acotado con truncate. */
export const treasuryColParty = 'w-[11rem]';

/** Emisión + vencimiento con hora (p. ej. 27/05/2026 00:16). */
export const treasuryColDates = 'w-[9.5rem] min-w-[9.5rem]';

export const treasuryColAging = 'w-[6.5rem]';

export const treasuryColBalance = 'w-[5.75rem]';

/** Cobros y pagos a proveedores (listados de movimientos). */
export const treasuryColPaymentDate = 'w-[7.25rem] min-w-[7.25rem]';

export const treasuryColMethod = 'w-[5.25rem]';

export const treasuryColReference = 'w-[6.5rem] max-w-[6.5rem]';

/** Adjunto / comprobante de pago (icono). */
export const treasuryColProof = 'w-[4.5rem]';
